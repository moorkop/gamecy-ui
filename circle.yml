machine:
  timezone:
    America/Denver

general:
  build_dir: dist

dependencies:
  cache_directories:
    - node_modules

notify:
  webhooks:
    - url: https://webhooks.gitter.im/e/035e6a1140f00a630984
